/**
 * Next.js configuration.
 *
 * Defines framework-level runtime and build settings.
 */

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
